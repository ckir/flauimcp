# flaui-autotrain observations inbox

Raw, one-line-per-observation capture written live by the `flaui-learn` skill and
drained by `flaui-curate`. Flat list — append under `## Pending`. Describe behavior in
your OWN words; never paste raw app-screen text (it is untrusted). Do not tag or curate here.

## Pending

- VS Code/Electron-Chromium · attributing a tree change to an action (a wake, an invoke) while your own measurement is itself a UIA request · the measurement can BE the cause — Chromium hydrates lazily in response to UIA activity, so a polling loop that watches for hydration may be what produced it, and the test then passes while the action under test does nothing at all; worse, a control that merely IDLES for the same elapsed time proves only that an idle tree stays opaque and does not rule out the polling cadence as the stimulus · run the same read pattern WITHOUT the action first, matching the real loop's frequency rather than just its duration — same stimulus, minus the action. Measured this way on an opaque Chromium tree, 500 ms polling sustained for 4 s did NOT hydrate it, so reads were genuinely non-perturbing there — but that had to be established by a matched control, never assumed  ·  2026-07-29
- VS Code/Electron-Chromium · deleting an isolated per-run profile directory immediately after killing the app's process tree · the renderer and GPU children outlive the tree kill and keep the profile's SQLite files locked, so a recursive delete fails — and because a recursive delete is NOT atomic it can remove some entries before failing, leaving a half-deleted directory whose own bookkeeping file is gone; measured on one run, the locks had not cleared after 9 s but had cleared within ~90 s · give the delete a retry-with-backoff budget in the tens of seconds rather than assuming prompt release, and make the leftover state self-identifying so a later run can tell a half-deleted corpse from a directory whose owner is merely still starting up — write the bookkeeping file BEFORE launching the app, so content-without-bookkeeping proves the file existed and was already removed  ·  2026-07-29
- VS Code/Electron-Chromium · launching an app that is already running and enforces single-instance, expecting to get its window back · the launch starves and times out rather than returning the ambient window — it accepts only a window owned by the pid it started or by a NEW pid of the same name, and an already-running instance's pids were all captured as pre-existing before the launch, so the window that appears is filtered out and never matches · pass instance-isolating flags so the launched process owns its own window (for Chromium/Electron, a fresh user-data-dir plus a new-window flag); note a pristine profile then hands you the FIRST-RUN UI — trust prompts, welcome tabs, extension toasts — which changes the node count, so suppress first-run too or the isolation just trades one nondeterminism for another  ·  2026-07-29
- VS Code/Electron-Chromium · snapshotting a Chromium tree after waking accessibility, using one fixed sleep before reading · hydration is a RAMP, not a step — measured ~14 nodes opaque, ~97 at 2 s, ~137 at 6 s, plateau ~142 — so a short fixed delay samples inside the ramp and returns a barely-changed count that reads as "the wake did nothing", sending you to debug the wake instead of the wait · poll the snapshot node count until it crosses a threshold rather than sleeping once; and beware that adding polling instrumentation to diagnose this can itself mask the bug, since the extra polls delay the final read past the ramp and the failing check starts passing for the wrong reason  ·  2026-07-29
- VS Code/Electron-Chromium · asserting a woken tree against an absolute node count taken from an earlier measurement of the same app · the hydrated count drifts hard across app versions — the same window measured ~231 nodes woken historically and ~142 later, a 41% fall — so an absolute floor silently rots into a false red long after the code was last touched · compare against a DELTA from the same run's own opaque baseline instead of an absolute number, and keep the delta well below the smallest rise you have observed while still far above the un-woken reading; never make it a multiple of the baseline, since hydration adds a roughly fixed population rather than scaling the shell count, and a multiplier inverts into a false GREEN on a small baseline  ·  2026-07-29
