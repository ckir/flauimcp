# flaui-autotrain observations inbox

Raw, one-line-per-observation capture written live by the `flaui-learn` skill and
drained by `flaui-curate`. Flat list — append under `## Pending`. Describe behavior in
your OWN words; never paste raw app-screen text (it is untrusted). Do not tag or curate here.

## Pending

- WPF/FlaUI-UIA3 (repo TestApp fixture) · any desktop_wait_for on this host — one full UIA tree walk costs ~3s, so a poll is nowhere near free · elapsedMs overshoots timeoutMs by multiples: a 1500ms budget returned at 5807ms and afforded exactly ONE poll, so a short timeout silently degrades from a polling wait into a single-shot check · NONE — measured through the C# path the MCP tool calls (BuildModelAsync), not by driving desktop_wait_for itself; the overshoot looks structural, not tunable  ·  2026-07-29

- Antigravity/Electron-Chromium · checking whether a GUI peer app is reachable before driving it · desktop_list_windows returns NO entry for the app at all while its process is very much alive — the window had been closed to tray/hidden, so a pure-Win32 top-level enumeration correctly sees nothing and it looks identical to "the app is not running" · cross-check with the OS process list and read MainWindowTitle: an alive process with an EMPTY MainWindowTitle means "running but no top-level window", which distinguishes a hidden/tray app from a dead one and from a broken tool — do not conclude the tool failed  ·  2026-07-29
- WindowsTerminal · reading a background tab by ordinal while OTHER WT windows/tabs exist on the desktop · the returned TEXT came from the intended tab but the reported tabTitle named a different one — so the title cannot be used to confirm which tab you actually read, and the mismatch is silent (no error, plausible-looking payload) · do not treat tabTitle as proof of identity when more than one WT window is open; confirm from the TEXT itself (a marker you put there) and read tabs when the desktop is otherwise quiet — the same read is correct when nothing else is competing  ·  2026-07-29
- WPF/FlaUI-UIA3 · coordinate-based input aimed at a window while a SECOND instance of the same app sits at the same default position · the action silently lands on the other window: a right-click produced NO context menu at all, so it presents as "the element never appeared" rather than as a mis-targeted click — and it is not a timing race, 2.5s of deterministic polling never surfaced the menu. Focusing the target window first did NOT prevent it · run one instance at a time for coordinate-based input, or target by element ref rather than screen coordinates. Mechanism is INFERRED by elimination (fails only alongside other instances, passes alone, immune to waiting) — a strong hypothesis, not a proven cause  ·  2026-07-29
- WindowsTerminal · closing a window you spawned and immediately re-listing to confirm it went · the close DOES take, but the window is still enumerable for seconds afterwards — WT tears down asynchronously while it reaps the child console processes — so an immediate confirmation read reports a false "it did not close", and a cleanup routine that trusts that single sample will escalate against a window that was already on its way out · poll the window list for ~15s before concluding a close failed; treat "still listed" as undecided rather than as failure, and never escalate on the first sample  ·  2026-07-30
- Unknown/any · picking which window to close by testing the Title from desktop_list_windows for a substring · title-alone matches unrelated apps — an editor, browser tab or file manager showing a FILE whose name contains your marker matches exactly as well as the window you meant, so an automated cleanup can close the human's own work and it looks like a successful tidy-up · always conjoin ProcessName from the SAME desktop_list_windows row with the title test before acting; the process filter is what makes a title substring safe, and a distinctive case-sensitive marker is not sufficient on its own  ·  2026-07-30
- WindowsTerminal · deciding whether synthetic input will work by trusting a human's earlier statement about console vs RDP · session state changes underneath you — a report of "I'm on RDP" was true when made and stale minutes later, and acting on it would have skipped work that was actually runnable (UIA and window enumeration work over RDP; only SendInput does not) · measure it with qwinsta and read the row marked with '>' — an Active console row with rdp-tcp merely Listening means synthetic input will deliver; this is one command and beats both asking and assuming  ·  2026-07-30
