# flaui-autotrain observations inbox

Raw, one-line-per-observation capture written live by the `flaui-learn` skill and
drained by `flaui-curate`. Flat list — append under `## Pending`. Describe behavior in
your OWN words; never paste raw app-screen text (it is untrusted). Do not tag or curate here.

## Pending

- WPF/FlaUI-UIA3 (repo TestApp fixture) · any desktop_wait_for on this host — one full UIA tree walk costs ~3s, so a poll is nowhere near free · elapsedMs overshoots timeoutMs by multiples: a 1500ms budget returned at 5807ms and afforded exactly ONE poll, so a short timeout silently degrades from a polling wait into a single-shot check · NONE — measured through the C# path the MCP tool calls (BuildModelAsync), not by driving desktop_wait_for itself; the overshoot looks structural, not tunable  ·  2026-07-29

- WindowsTerminal · reading a background tab by ordinal while OTHER WT windows/tabs exist on the desktop · the returned TEXT came from the intended tab but the reported tabTitle named a different one — so the title cannot be used to confirm which tab you actually read, and the mismatch is silent (no error, plausible-looking payload) · do not treat tabTitle as proof of identity when more than one WT window is open; confirm from the TEXT itself (a marker you put there) and read tabs when the desktop is otherwise quiet — the same read is correct when nothing else is competing  ·  2026-07-29
- WPF/FlaUI-UIA3 · coordinate-based input aimed at a window while a SECOND instance of the same app sits at the same default position · the action silently lands on the other window: a right-click produced NO context menu at all, so it presents as "the element never appeared" rather than as a mis-targeted click — and it is not a timing race, 2.5s of deterministic polling never surfaced the menu. Focusing the target window first did NOT prevent it · run one instance at a time for coordinate-based input, or target by element ref rather than screen coordinates. Mechanism is INFERRED by elimination (fails only alongside other instances, passes alone, immune to waiting) — a strong hypothesis, not a proven cause  ·  2026-07-29

- Notepad (Win11 store build)/WinUI-RichEdit · desktop_type of a 56-char mixed alpha+digit string at the DEFAULT pacing (interKeyDelayMs=15) into a blank tab's document body · no failure observed - the text landed byte-exact, desktop_type's own verify reported ran/verified true with mismatch false, and an independent desktop_get_text matched all 56 chars · NONE NEEDED (nothing to recover from) - the reactive-editor garble the manual warns about did NOT reproduce at default pacing on this build, so treat paced typing as sufficient here and reach for set_value/paste_text only on a real mismatch; a 0-delay contrast run was NOT attempted, so this says nothing about the raw atomic blast  ·  2026-09-09

- Notepad (Win11 store build)/WinUI · desktop_launch_app on notepad.exe when the user previously left unsaved tabs open · the freshly launched window is NOT the blank editor the driving step assumes - Notepad's session restore reopens the prior tabs, so the new process comes up already holding the user's real unsaved documents (here 9 tabs, 8 flagged modified); a step that assumes launch==blank and ends by closing the window would force-close that unsaved work, and it reads as a successful tidy-up · work in an isolated tab you create yourself and close only that tab, never the window; and do not trust a 'it attached to an already-running instance' reading of this - measured, the pid's process StartTime was 2 min old and only ONE Notepad process existed, so the tabs came from RESTORE, not from attaching. Check StartTime before believing either story  ·  2026-09-09


- Explorer/WinUI context-menu popup · sending desktop_key with a window handle but NO ref while a PopupHost menu is open · the chord does not reach the menu - it lands on the owning Explorer window's own Pane, the highlight never moves, and the call still returns ok:true, so nothing signals the misroute (desktop_get_focused_element afterwards named a Pane in the Explorer window, not a menu item) · do not steer one of these menus by bare chords; resolve the MenuItem with desktop_find and invoke/click that ref instead. NOTE: this leg was measured by a dispatched subagent and I did NOT re-measure it myself, unlike the snapshot/find result above which I reproduced first-hand  ·  2026-09-09


- WindowsTerminal · running the terminal-tab-app-find curriculum task on a desktop that happens to hold no ambiguously-titled tab · the task reports 'Failure-Mode: none observed' and looks like a clean pass, but its TRAP was never exercised at all - the box had exactly 2 tabs, one titled by its guest program and one an idle cmd.exe with an empty buffer, so there was no launcher-titled tab hosting a real CLI app to misidentify. A future reader would wrongly conclude the title-is-not-a-filter trap does not reproduce · record UNEXERCISED separately from PASSED, and state the precondition a task needs (here: a tab whose title names the launcher while a different program runs inside it) so the next run can set it up rather than re-derive a false negative. Positive facts that DID land: read_terminal_tab restored the originally active tab with restoreConfidence high, and the reported tabTitle DID match the requested tabIndex - consistent with the existing note that the title mismatch shows up only when other WT windows compete, which was not the case here, so this neither confirms nor refutes that one  ·  2026-09-09


- Explorer/WinUI virtualized list · holding an element ref across any re-read of a scrolling list · refs for the SAME item (stable automationId '0', item-000.txt) were reissued on every single re-read - e30 -> e177 -> e196 -> e217 -> e222 -> e227 - not merely after a scroll but across successive find calls · never cache a ref across a re-read of a virtualized list; re-find immediately before acting, or target by selector/automationId which resolves fresh at call time  ·  2026-09-09



- Explorer/Shell folder view (Win11), flaui-mcp driven from a background server process · focusing a VISIBLE, non-minimized, non-foreground window to reproduce the documented foreground-lock · the lock did NOT occur - desktop_focus_window returned foregroundGained:true and the window took foreground, twice, though the manual states unconditionally that a background-process server cannot SetForegroundWindow an existing window past the active one · UNCONFIRMED MECHANISM, not isolated: the whole run happened at the PHYSICAL console with the operator present, an active input lease, and synthetic input having fired moments earlier, so recent REAL human input is the obvious suspect - it lines up with the earlier measured note that Windows refuses a foreground change to a process with NO recent real human input. Treat the foreground-lock as CONDITIONAL on input recency rather than as a property of being a background process, and re-test it on an idle unattended box before trusting either wording  ·  2026-09-09

- VS Code/Electron-Chromium · measuring an opaque Chromium window's PRE-WAKE baseline by walking it (desktop_snapshot or snapshot_stats) before calling wake_accessibility · THE OBSERVATION DESTROYS THE STATE - the first UIA walk of a cold window returns the opaque reading AND triggers the lazy hydration, so the next walk seconds later shows a full tree with NO wake having been called. Measured: my cold snapshot_stats read total 14 (Window1/Pane10/Button3, no wakeable flag consumed yet); seconds later, after further walks and still BEFORE any wake, the baseline read 199; the subsequent wake_accessibility then produced delta 0 and a flat 500ms control series, both simply because hydration had already finished. An experiment shaped 'snapshot to confirm opaque -> wake -> snapshot' therefore CREDITS THE WAKE with hydration its own baseline walk caused · take the opaque reading ONCE and treat it as consumed; judge a wake by what becomes ADDRESSABLE (find/invoke a real control) rather than by a second node count, or use a window whose provider is provably cold. ⚠ WHICH walk triggered it is NOT ISOLATED (my stats call vs the next agent's snapshot); what IS measured is that hydration completed BEFORE the wake. Also measured: once warm, minimize+restore does NOT return the window to the opaque reading - so a cold provider, not window visibility, is the precondition, and this sits in tension with the existing note that 500ms polling for 4s did not hydrate an opaque tree, which now needs a re-test  ·  2026-09-09

- VS Code/Electron-Chromium · wake_accessibility lifecycle on an already-hydrated window · no failure - wake returned {wakeId k1, alreadyAwake:false} on a tree that was already full, a second wake returned the SAME wakeId with alreadyAwake:true, list_wakes showed the single held wake, release_accessibility succeeded and list_wakes went empty · idempotency and release behave as documented; note alreadyAwake reports whether THIS SERVER holds a wake, NOT whether the tree is hydrated - it said false on a fully-hydrated 199-node window, so it is a lease-style bookkeeping flag and must not be read as a hydration probe  ·  2026-09-09

- Settings/WinUI (ApplicationFrameHost) Windows Update page · running the dynamic-loading-ghost-ui task against a page the operator opened minutes earlier · trap UNEXERCISED, not passed - the page had fully settled ('up to date', timestamp populated, every control enabled, no placeholders), so there was no ghost UI to observe. Worth noting HOW that was established: desktop_snapshot_diff across the whole ~19s probe returned added/removed/changed all EMPTY, which is real evidence of a static page rather than a single glance that happened to look settled · to actually exercise this trap the page must be caught DURING a fetch, which needs input (clicking Check for updates) and so is an input-tier task, not a destructive:false one - the curriculum entry currently assumes a slow page will still be loading when you attach, and on a fast machine it will not be  ·  2026-09-09

- Settings/WinUI · giving desktop_wait_for a timeout shorter than one full tree walk · elapsedMs overshoots the budget by MULTIPLES and the wait silently degrades into a single-shot check: measured timeoutMs 3000 -> satisfied:true at elapsedMs 7934 for an ALREADY-PRESENT control (2.6x), and timeoutMs 2000 -> satisfied:false at elapsedMs 8817 (4.4x), no throw either time · budget these waits in WALKS, not milliseconds - a timeout below one walk buys exactly one poll. NOT a new defect: desktop_wait_for's own tool description already states the budget is checked only BETWEEN polls and that a short budget becomes a single-shot check, so this is documented behaviour independently reproduced. The value here is that it now reproduces on a SECOND, different app (Settings/WinUI) after first being measured on the WPF test fixture, so it is a property of the walk cost, not of one app  ·  2026-09-09

- Settings/WinUI · trying to compare OCR wait cost against UIA wait cost from the tools' return values · you cannot - desktop_wait_for_text returns {satisfied:false} on timeout with NO elapsedMs field, while desktop_wait_for returns elapsedMs. Verified against the tool descriptions: wait_for_text's documented shapes are {satisfied:false} and {satisfied:true, match:{...}}, so the absence is BY DESIGN, not a bug · if you need the OCR wait's real cost, time the call yourself; and note wait_for_text throttles to >= 750ms between OCR passes, so its budget arithmetic differs from wait_for's walk-bound one  ·  2026-09-09

- WindowsTerminal / any window, flaui-mcp driven from a background server · calling desktop_focus_window after the interactive session changed underneath the agent (physical console -> RDP reconnect) · the documented FOREGROUND-LOCK finally appeared, having refused to reproduce earlier the same day: {ok:true, foregroundGained:false, currentForeground:{handle:'0'}} - handle 0 meaning NO window owns foreground at all - plus the recommendedAction 'call-wait-for-foreground' and recovery fields that the earlier probe reported as ABSENT on its successful calls. desktop_wait_for_foreground then returned {foregroundGained:false, reason:'timeout', currentForeground:{handle:'0'}} across a 20s budget · when focus will not land, check the SESSION before re-looping the wait: qwinsta showed the session had moved back to '>rdp-tcp#0 Active' with console demoted to Conn, and the display had changed resolution and an app had a new pid - all signs the desktop under you is not the one you measured on. ⚠ CONFOUNDED, mechanism still NOT isolated: this supports the earlier hypothesis that the lock is conditional on recent real human input rather than on being a background process, but a session change is a second variable moving at the same time, so it corroborates rather than proves  ·  2026-09-09

- Chrome + VS Code (Chromium/Electron) · two-arm cold-provider experiment to find out what actually hydrates an opaque tree · **wake_accessibility contributes NOTHING to hydration - the FIRST UIA WALK is the trigger.** Each arm got a genuinely cold provider (Chrome killed and relaunched with a fresh --user-data-dir on a local page holding ~20 DOM elements). Arm A, NO wake: walk1 = 39 nodes with no DOM, walk2 = 63 with the DOM fully present (Hyperlink 2, ListItem 4, Text 7, Edit 1->3, Button 13->16). Arm B, wake FIRST ({wakeId k2, alreadyAwake:false}) then walk: walk1 = 39, walk2 = 63 - the SAME counts and the same control-type histogram as the arm with no wake at all. It generalises to Electron: a cold VS Code (new pid, never walked) went 14 -> 194 on its second walk with no wake · stop treating a wake as the thing that reveals content. Walk once to trigger, walk again to read, and judge a wake by ADDRESSABILITY rather than by a second node count. ⚠ Scope: the Electron arm had no wake-first control (one cold shot, on the operator's live editor), so there it is proven that a walk alone hydrates, NOT that a wake adds nothing  ·  2026-09-09

- Chrome (Chromium) · believing the shipped rule that a wake reaches the browser CHROME but not the page DOM, so page content needs find_text/OCR · the page DOM DOES reach UIA and is precisely addressable - after hydration desktop_find name:'ProbeButtonTwo' returned one match with automationId 'b2' (the literal HTML id), controlType Button and real on-screen bounds, no OCR anywhere · target Chromium page content by name/automationId like any other control; keep find_text for surfaces that genuinely expose nothing. This also settles the earlier UNISOLATED question of which walk hydrated a window between two measurements: any first walk does. OPEN and explicitly NOT measured - what a wake is then FOR; the tool description says the wake is HELD and that releasing it lets Chromium re-collapse lazily, so persistence rather than initial hydration is the plausible purpose, but re-collapse timing was never tested  ·  2026-09-09

- WindowsTerminal · trying to BUILD the title-is-not-a-filter trap on purpose by launching a program through `wt.exe` · you cannot get a launcher-only tab title that way - WT titles the tab from the COMMAND LINE it was given, so `wt ... cmd.exe /k python -i` produced the tab title 'cmd.exe - python  -i', naming the very program the trap is supposed to hide, and an explicit `--title "cmd.exe"` did NOT override it (both tabs still showed the commandline-derived title) · the bare-launcher title that makes the trap real (e.g. 'C:\WINDOWS\system32\cmd.exe') arises when a shell is started PLAIN and the program is launched by TYPING inside it - so reproducing this trap deliberately needs an input lease with --allow-shells, which makes it an INPUT-tier fixture even though the curriculum files the task as lease-exempt. Observing it in the wild stays lease-free; only manufacturing it needs input  ·  2026-09-09

- WindowsTerminal launched from PowerShell · passing a multi-command wt argument string containing `;` via Start-Process -ArgumentList · the `;` did not stay inside the wt command line: instead of ONE window with two tabs (which the same shape produced on a previous invocation) it opened TWO separate WT windows with identical titles and identical tab layouts, which is easy to misread as a tool bug or as a failed close · after launching a wt fixture, always ENUMERATE and count the WT windows before acting - and note that all WT windows share ONE pid (here 14940, the same pid as the operator's own terminal), so pid can never distinguish them; close by window HANDLE only. Independently useful: this makes two identically-titled WT windows trivial to produce, which is the precondition the older tabTitle-mismatch observation needs  ·  2026-09-09

- WindowsTerminal · closing a WT window whose tabs hold EXITED processes · every WINDOW-level close path reports success and does nothing: desktop_close_window {ok:true} x3, desktop_invoke on the titlebar Close button {ok:true,pathUsed:'pattern'}, and even a real synthetic desktop_click on that button {ok:true} - the window stayed enumerable through all of it, its handle re-minted on every poll (w23/w24 -> w25/w26 -> w27/w28) which reads as progress when nothing is happening · RECOVERY FOUND, and it is per-TAB, not per-window: desktop_invoke each tab's own 'Close Tab' button (automationId CloseButton, [Invoke]) - closing the last tab took the window with it, first try. Get the refs from a plain desktop_snapshot of the WT window. ⚠ Two traps found while chasing this: (a) a click on a NON-foreground window is consumed as ACTIVATION, so the earlier attempts never pressed anything - focus first, then click; (b) the PopupHost that appeared after the click was NOT a confirm dialog, it was the button's hover TOOLTIP (4 nodes: Pane -> Window Popup -> ToolTip 'Close' -> Text), so do not read 'a popup appeared' as 'a dialog is waiting'. ⚠ SUPERSEDES my own earlier capture of this same session, which called it a close_window honesty defect - the window was closable all along by the tab route. And the process-kill escape hatch really is barred: all WT windows share ONE pid, here the agent's own session's  ·  2026-09-09

- WindowsTerminal / any app · passing desktop_key BOTH a window and a ref whose element cannot take focus (here the titlebar Close Button) · it fails {error:'INTERNAL', message:'Member not found. (0x80020003 (DISP_E_MEMBERNOTFOUND))'} - a raw COM error surfaced verbatim, and its suggestedRecovery 're-check arguments and retry' points at the wrong thing entirely: the arguments were valid, the element simply has no focus support, so retrying identically can only fail again · pick a ref the snapshot marks {focusable} (the terminal body Text node is), or omit ref/window to target the foreground window - but if you do that with a destructive chord like Alt+F4, re-verify IsForeground in the immediately preceding call, because on this box several windows of the SAME process and title can be one ordinal apart  ·  2026-09-09

- WindowsTerminal · deciding whether a tab's title can be trusted to name the program running in it · the trap is REAL but SHELL-DEPENDENT, and the shipped wording 'a tab title names the LAUNCHER' is too broad. Measured by typing the SAME command into two plain shells at the physical console: under cmd.exe the tab title REWROTE ITSELF from 'cmd.exe' to 'cmd.exe - python  -i' the moment the child started, so the program is plainly visible and there is no trap; under powershell.exe the title stayed 'powershell.exe' while a Python REPL ran inside it, and read_terminal_tab returned tabTitle 'powershell.exe' next to buffer text 'Python 3.14.5 ... >>>' · the tab title tracks the CONSOLE TITLE, and cmd rewrites that per child while PowerShell does not - so treat a cmd-titled tab as informative and a powershell/pwsh-titled tab as saying NOTHING about what runs inside. A TUI that sets its own console title (Claude Code shows a spinner glyph plus its name) is informative again. Read the buffer for the powershell case; that is where the original hidden-agent incident lives  ·  2026-09-09

- WindowsTerminal · desktop_get_text on a WT terminal body by ref · fails AmbiguousMatch - 'Ref e2 matches 2 elements by Name + control type' - because WT names BOTH the tab HeaderTextBlock and the buffer body Text with the same string (here 'powershell.exe'), so the two are indistinguishable by name+type even though their bounds differ hugely (46x16 header vs 1113x586 body) · do not read a WT buffer by ref; use desktop_read_terminal_tab, which targets by tabIndex and sidesteps the ambiguity entirely (it also restored the original tab, restoreConfidence high). The generic lesson: a fail-closed ref read can be caused by the APP naming two very different elements identically, not by anything you did wrong  ·  2026-09-09
